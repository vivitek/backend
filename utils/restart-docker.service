[Unit]
Description=Update and restart vivi's server container
StartLimitIntervalSec=0

[Service]
Type=simple
Restart=on-failure
RestartSec=1
User=root
ExecStart=sh {{EXEC_PATH}}/webhook-handler.sh


[Install]
WantedBy=multi-user.target
