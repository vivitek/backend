sudo: false
language: node_js
node_js:
- '12'
branches:
  only:
  - master
services:
    - docker
cache:
  directories:
  - node_modules
before_install:
- docker-compose up --build
- npm update
install:
- npm install
script:
- npm test
- npm run coveralls
